import Input from 'src/components/Home/Input';

import './style.scss';

// This component contains gift finder form container of the homepage
const Form = () => (
  <Input className="inputsContainer" />

);

export default Form;
